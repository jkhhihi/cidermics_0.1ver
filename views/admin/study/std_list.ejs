<!DOCTYPE html>
<html>
  <head>
  	<meta charset="utf-8">
  	<meta name="viewport" content="width=device-width, initial-scale=1"-->
  	
    <title>Cidermics Admin</title>
    <link rel='stylesheet' href='/css/materialize.css' />
    <link rel='stylesheet' href='/css/style.css' />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    
    <script src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
	  <script src="/js/materialize.js"></script>
    <script type="text/javascript" src="/smart_editor/js/HuskyEZCreator.js" charset="utf-8"></script>
    <script>
    $( document ).ready(function(){
    	$(".button-collapse").sideNav();
    })
    </script>
  </head>
  <body onload="rdate()">
  	<% var data= CP; %>
    <% include ../include/nav.ejs %>

    <a href="/adm/study/" class="waves-effect waves-light btn modal-trigger">뒤로가기</a>
    
  <div class="container">
    <div class="row">
      <p><h5><% if(stdlist[0].cate==1){%>강의 리스트<% }else if(stdlist[0].cate==2){ %> 스터디 리스트 <%}else{ %> 온라인 <% } %></h5></p>
    </div>
    <div class="row">
			<table>
        <thead>
          <tr>
              <th>idx번호</th>
              <th>시즌</th>
              <th>강의/스터디명</th>
              <th>리더명</th>
              <th>게시일</th>
          </tr>
        </thead>

        <tbody>
          <% for(var i = 0; i< stdlist.length; i++) { %>
            <tr>
              <input type="hidden" name="idx" value="<%= stdlist[i].idx %>" />
              <td><%= stdlist[i].idx %></td>
              <td><%= stdlist[i].decate %></td>
              <td><a href="/adm/study/detail/<%= stdlist[i].idx %>"><%= stdlist[i].subject %></td>
              <td><%= stdlist[i].leader %></td>
              <td><%= stdlist[i].regdate %></td>
              <!--td><p class="acss"><a href="/adm/stdlist/delete/<%= stdlist[i].idx %>">삭제</a></p></td-->
            </tr>
          <% } %>

        </tbody>
      </table>
		</div>
  </div>
</body>
</html>
